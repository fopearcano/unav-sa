# tools/

Developer and operational tooling for UNAV-SA — maintenance scripts, data
utilities and CLI helpers that support development but are **not** part of the
shipped `unav_core` / `unav_app` / `unav_server` packages.

(Anything user-facing and reusable belongs in the core or app, not here.)

Empty for now — populated as the project grows.
