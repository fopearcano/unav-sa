# UNAV-SA runtime / core dependencies (conservative).
# These mirror [project.dependencies] in pyproject.toml.
#
# Catalog-access (astroquery, pyvo) and server (fastapi, uvicorn) stacks are
# intentionally optional and live in pyproject.toml extras — install them with:
#   pip install -e ".[query]"      # Gaia/SDSS/DESI/SIMBAD/VizieR/MAST access
#   pip install -e ".[server]"     # local API server

astropy>=6.0
numpy>=1.24
pydantic>=2.5
SQLAlchemy>=2.0
typer>=0.12
rich>=13.0
