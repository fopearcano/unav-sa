"""Application UI: search, inspection and navigator controls.

Views and widgets for **catalog search**, **object inspection**, **navigator
control** and **route/mission** editing. Talks to :mod:`unav_core` (directly or
via :mod:`unav_server`). Not implemented yet.
"""
